name: Build Flet APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4 # Aggiornato a v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Python
        uses: actions/setup-python@v5 # Aggiornato a v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install flet requests beautifulsoup4

      - name: Build APK
        run: |
          flet build apk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4 # <--- QUESTA È LA CORREZIONE CRITICA
        with:
          name: CalcioPremium-APK
          path: build/apk/*.apk
